const Events = require('../../../../models/events');
const mongodb = require('../../../../libs/mongodb');

// TODO secure from ddos, add captcha
async function joinEvent (eventKey) {
	return {};
}

module.exports = joinEvent;
